const Discord = require('discord.js')

module.exports = {
    name: 'bot',
    description: "ester egg",
    async execute(client, message, cmd, args) {

        message.reply('Mi spiace ma tu sei più bot di me')
        
    }
}